좋아. 이제 bookmark_items 조회가 정상적으로 완료되었으니,
다음 단계로 로그인한 사용자가 bookmark_items를 자신의 북마크로 담고 취소할 수 있는 기능을 구현해줘.

현재 DB 구조와 RLS는 이미 완료되어 있으므로 변경하지 않는다.

이번 단계의 목표:

"바로가기 링크의 북마크 버튼을 누르면 현재 로그인한 사용자의 user_bookmarks에 저장되고,
다시 누르면 해당 사용자의 북마크에서 삭제되는 기능"

구현 요구사항:

1. 현재 js/lib/supabase.js의 기존 Supabase 클라이언트를 재사용한다.
   새로운 Supabase 클라이언트를 만들지 않는다.

2. 로그인한 현재 사용자를 Supabase Auth 세션에서 확인한다.
   현재 AuthContext에서 관리하는 실제 Supabase 세션/user 정보를 재사용할 수 있다면 그렇게 한다.
   별도의 가짜 user_id나 localStorage 기반 사용자 식별을 만들지 않는다.

3. user_bookmarks에 북마크를 추가할 때 다음 데이터를 INSERT한다.

- user_id: 현재 로그인한 Supabase Auth 사용자의 id
- item_id: 현재 선택한 bookmark_items.id
- folder_id: 이번 단계에서는 NULL

4. 북마크 취소 시 user_bookmarks에서 해당 사용자의 해당 item만 DELETE한다.

삭제 조건은 반드시 현재 로그인한 사용자와 item을 함께 기준으로 한다.

예:
user_id = 현재 사용자 id
item_id = 선택한 bookmark_items.id

다른 사용자의 북마크가 삭제될 수 있는 구조를 만들지 않는다.

5. 기존 user_bookmarks의 UNIQUE(user_id, item_id) 제약을 활용한다.
   프론트엔드에서 중복 INSERT가 발생하지 않도록 상태를 관리하되,
   DB의 UNIQUE 제약을 제거하거나 변경하지 않는다.

6. HomePage에서 현재 바로가기 링크 각각의 북마크 상태를 표시할 수 있도록 구현한다.

페이지가 로드되면 로그인한 사용자의 user_bookmarks를 SELECT해서
현재 bookmark_items 중 어떤 항목을 이미 북마크했는지 확인한다.

조회할 때 필요한 최소 컬럼만 사용한다.
예를 들어 item_id를 기준으로 상태를 관리할 수 있다.

7. 비로그인 사용자의 동작도 명확하게 처리한다.

비로그인 상태에서 북마크 버튼을 누르면
user_bookmarks INSERT를 시도하지 말고,
현재 서비스의 로그인 UI/UX에 맞는 방식으로 로그인 필요 안내를 보여준다.

기존 로그인/회원가입 페이지로 이동시키는 방식이 기존 구조에 자연스럽다면 그렇게 처리한다.

8. 기존 UI 디자인은 최대한 유지한다.

현재 LinkCard의 북마크 버튼 디자인과 위치를 유지하고,
북마크 여부에 따라 기존 아이콘/스타일을 활용할 수 있다면 활용한다.

9. 현재 localStorage를 사용하던 linkBookmarks 기능은
   Supabase user_bookmarks 기능으로 완전히 대체한다.

기존 mime.linkBookmarks를 새로운 사용자 북마크 기능과 동시에 사용하지 않는다.
다만 다른 기능에서 해당 localStorage 값을 참조하고 있다면 먼저 찾아서 보고하고,
임의로 삭제하지 말고 이번 변경 범위를 벗어나는 부분은 알려줘.

10. "내 북마크" 영역도 현재 구조를 확인해서 처리한다.

현재 HomePage에 있는 "내 북마크"가 기존 localStorage 기반 linkBookmarks를 사용하고 있다면,
이번 단계에서 이 영역도 Supabase user_bookmarks를 기반으로 표시하도록 변경할 수 있는지 분석해줘.

다만 바로가기 카드의 북마크 기능과 내 북마크 영역의 연결에 필요한 변경만 진행하고,
행사 프로그램(programData)의 "내 일정" 기능은 건드리지 않는다.

11. 폴더 기능은 이번 단계에서 구현하지 않는다.

- folder_id는 NULL로 저장
- 폴더 선택 UI 추가하지 않음
- folders 테이블 조회하지 않음
- 폴더 생성/수정/삭제 구현하지 않음

12. DB는 절대 수정하지 않는다.

- bookmark_items 변경 금지
- user_bookmarks 변경 금지
- folders 변경 금지
- FK 변경 금지
- UNIQUE 변경 금지
- RLS 변경 금지
- migration 추가 금지

13. 테스트를 반드시 진행한다.

로그인한 테스트 계정으로:

1. 북마크하지 않은 링크 → 북마크 클릭
   → user_bookmarks에 INSERT 되는지 확인

2. 같은 링크를 다시 클릭
   → 해당 row가 DELETE 되는지 확인

3. 페이지 새로고침
   → DB에 저장된 북마크 상태가 다시 표시되는지 확인

4. 다른 링크도 추가
   → 여러 bookmark_items를 정상적으로 저장할 수 있는지 확인

5. 로그아웃 후 비로그인 상태에서 북마크 클릭
   → INSERT가 발생하지 않고 로그인 필요 안내가 나오는지 확인

6. 로그인한 상태에서 다른 사용자의 user_bookmarks를 조회하거나 삭제할 수 없는지
   → 기존 RLS가 정상적으로 작동하는지 확인

7. 기존 "바로가기" 카테고리 필터가 북마크 기능 추가 후에도 정상 작동하는지 확인

8. npm run build가 정상적으로 통과하는지 확인

중요:
이번 단계에서는 폴더 기능을 구현하지 않는다.
DB 스키마/RLS도 변경하지 않는다.

작업 후 반드시 다음을 보고해줘.

- 수정한 파일
- 각 파일의 변경 내용
- user_bookmarks INSERT/DELETE 구현 방식
- 로그인 사용자 식별 방식
- 비로그인 상태 처리 방식
- 기존 localStorage linkBookmarks가 어떻게 처리되었는지
- 실제 Supabase에서 INSERT/DELETE가 성공했는지
- 새로고침 후 상태 복원 테스트 결과
- RLS 테스트 결과
- npm run build 결과
