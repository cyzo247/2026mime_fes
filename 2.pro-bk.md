다음 단계로 사용자별 북마크 폴더 기능을 구현하려고 합니다.

현재까지 구현된 상태를 먼저 전제로 삼아주세요.

- Supabase Auth 회원가입/로그인/로그아웃/세션 유지 구현 완료
- bookmark_items는 Supabase에서 조회
- bookmark_items의 RLS 적용 완료
- folders 테이블 및 RLS 적용 완료
- user_bookmarks 테이블 및 RLS 적용 완료
- user_bookmarks.folder_id → folders.id FK 존재
- folders.user_id → auth.users.id FK 존재
- user_bookmarks.user_id → auth.users.id FK 존재
- user_bookmarks INSERT/UPDATE 시 자신의 폴더인지 RLS에서 검증
- 현재 HomePage의 링크 북마크는 user_bookmarks에 저장되고 있음
- 현재 링크를 새로 북마크하면 folder_id는 항상 null
- "내 북마크" 영역은 Supabase의 user_bookmarks와 bookmark_items를 이용해 표시되고 있음
- "내 일정" 기능은 이번 작업 범위에서 제외
- bookmark_items의 카탈로그 데이터와 카테고리 기능은 이미 정상 동작 중

이번 단계에서는 아직 코드를 수정하거나 DB를 변경하지 마세요.

먼저 현재 프로젝트의 북마크 관련 UI와 상태 구조를 분석하고,
기존 디자인을 최대한 유지하면서 사용자별 폴더 기능을 추가하려면 어떤 구조가 가장 적절한지 설계안을 작성해주세요.

특히 다음을 확인해주세요.

1. 현재 "내 북마크" 영역에서 폴더를 어떤 UI로 추가하는 것이 자연스러운지
2. 사용자가 폴더를 생성할 수 있는 위치와 UI
3. 폴더 이름 변경/삭제가 필요한지, 필요하다면 어떤 방식이 적절한지
4. 북마크를 추가할 때 folder_id를 선택할 수 있는 UI
5. 이미 북마크된 링크의 폴더를 변경할 수 있는 UI
6. folder_id가 null인 북마크를 어떻게 표시할지
7. 폴더가 여러 개일 때 북마크 목록을 어떻게 보여줄지
8. 폴더 삭제 시 해당 폴더에 들어 있던 user_bookmarks를 어떻게 처리할지
    - 북마크 자체를 삭제할지
    - folder_id만 null로 만들지
      둘 중 어떤 방식이 현재 DB 구조와 UX에 더 적합한지 판단해주세요.
9. 비로그인 사용자가 폴더 관련 UI를 볼 수 있는지, 볼 수 있다면 어떤 식으로 처리할지
10. 현재 HomePage.jsx의 구조를 기준으로 필요한 상태값, 함수, Supabase 쿼리를 어떤 식으로 추가해야 하는지
11. 기존 북마크 기능이나 "내 일정" 기능에 영향을 주지 않으면서 구현할 수 있는지
12. 추가적인 DB 스키마 변경이 정말 필요한지 여부

중요:

- 이번 단계에서는 설계와 분석만 하고 코드는 수정하지 마세요.
- 기존 DB의 테이블, FK, RLS 정책은 변경하지 마세요.
- 새로운 테이블이나 migration도 만들지 마세요.
- 현재 UI 디자인을 최대한 유지하는 방향으로 생각해주세요.

분석이 끝나면
① 추천 UI 구조
② 사용자 동작 흐름
③ 필요한 프론트엔드 변경사항
④ 필요한 Supabase 쿼리
⑤ DB 변경 필요 여부
⑥ 구현 시 주의할 보안/RLS 사항
순서로 정리해서 보고해주세요.
