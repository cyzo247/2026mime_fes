다음 작업으로 북마크 폴더 기능을 실제 구현해줘.

현재까지의 DB/RLS 및 북마크 기능은 정상적으로 구현·검증된 상태다.
이번 작업은 프론트엔드 구현만 진행하며, DB 스키마/RLS/FK에는 변경을 가하지 않는다.

==================================================
[1. 구현 범위]
==================================================

현재 HomePage.jsx의 기존 UI 구조와 스타일을 최대한 유지하면서
folders + user_bookmarks.folder_id를 이용한 개인 북마크 폴더 기능을 추가한다.

기존 ShareModal, 카테고리 필터, BookmarkedLinkRow 등의 UI 패턴을 최대한 재사용한다.

DB migration이나 SQL 변경이 필요하다고 판단되는 경우에는
임의로 실행하지 말고 먼저 이유와 변경안을 보고한 뒤 중단한다.

==================================================
[2. 북마크 섹션 폴더 UI]
==================================================

"북마크" 섹션에 기존 "바로가기" 카테고리 필터와 동일한 스타일의
폴더 필터 칩 행을 추가한다.

형태:

[전체] [폴더 없음] [사용자 폴더들...] [+ 새 폴더]

규칙:

- "전체"는 기본 선택 상태
- "폴더 없음"은 folder_id === null인 북마크를 표시
- 이후 사용자가 생성한 폴더들을 표시
- 마지막에 "+ 새 폴더"를 표시
- "+ 새 폴더"는 일반 폴더 칩과 시각적으로 구분되는 점선 테두리 스타일을 사용
- 기존 바로가기의 카테고리 필터 스타일을 최대한 재사용
- 바로가기의 linkCategory와 북마크의 activeFolderFilter는 완전히 별개의 상태로 관리

특정 폴더를 선택하면 해당 폴더에 속한 북마크만 표시한다.

그룹 헤더를 추가하는 방식이 아니라 기존 BookmarkedLinkRow 리스트 구조를
그대로 유지하면서 클라이언트 필터링 방식으로 구현한다.

==================================================
[3. 폴더 상태]
==================================================

HomePage.jsx에 필요한 상태를 추가한다.

예:

- folders
- foldersLoading
- foldersError
- activeFolderFilter
- folderModal

folderModal은 다음과 같은 형태를 사용할 수 있다.

{
mode: "create" | "rename" | "delete",
target: folder | null
}

기존 코드 스타일에 더 적합한 구조가 있다면 그 구조를 사용해도 되지만,
불필요하게 상태를 복잡하게 만들지는 않는다.

==================================================
[4. user_bookmarks 조회 변경]
==================================================

현재 user_bookmarks 조회는 item_id만 가져오고 있다.

이를 다음처럼 변경한다.

select("item_id, folder_id")

따라서 기존 linkBookmarks가 단순한 ID 배열이었다면
폴더 정보를 함께 보관할 수 있도록 구조를 변경한다.

예:

[
{
item_id: 2,
folder_id: null
},
{
item_id: 3,
folder_id: 5
}
]

기존 LinkCard의 북마크 여부 확인 로직은 이에 맞게 수정한다.

예:

linkBookmarks.some(bookmark => bookmark.item_id === link.id)

기존 북마크 추가/삭제 기능은 그대로 유지한다.

==================================================
[5. folders 조회]
==================================================

로그인한 사용자일 때만 folders를 조회한다.

쿼리는 다음 목적을 만족하도록 구현한다.

- id
- name
- created_at
- created_at 순서

RLS가 이미 auth.uid() = user_id로 보호하고 있으므로
현재 로그인 사용자의 폴더만 반환된다.

비로그인 상태에서는 folders 관련 Supabase 쿼리를 실행하지 않는다.

==================================================
[6. 폴더 생성]
==================================================

"+ 새 폴더" 클릭 시 FolderModal을 표시한다.

기존 ShareModal의 모달 패턴을 최대한 재사용한다.

모달에는:

- 폴더명 입력창
- 취소
- 만들기

를 제공한다.

폴더명 검증:

- 빈 문자열 금지
- 공백만 입력한 경우 금지
- 적절한 최대 길이 제한
- 저장 전에 trim()

폴더 생성 시:

folders INSERT

{
user_id: user.id,
name: trimmedName
}

성공하면:

- folders 목록 즉시 갱신
- 새 폴더가 UI에 표시
- 적절한 성공 토스트 표시
- 모달 닫기

user.id는 반드시 현재 Supabase Auth 세션의 실제 사용자 ID를 사용한다.

==================================================
[7. 폴더 이름 변경]
==================================================

선택된 폴더가 있을 때만 연필 아이콘을 표시한다.

위치는 기존 북마크 섹션의

"북마크 N개 · 내 계정에 저장됨"

부분을 활용하는 방향으로 구현한다.

특정 폴더를 선택했을 때:

"이 폴더: {폴더 이름}"

- 연필 아이콘
- 휴지통 아이콘

을 표시한다.

연필 아이콘 클릭:

- 동일한 FolderModal을 rename 모드로 사용
- 기존 폴더명을 input에 미리 입력
- 저장 시 folders UPDATE
- 현재 사용자 + folder id 조건을 사용
- 성공 시 폴더 목록과 현재 선택 상태를 즉시 갱신
- 성공 토스트 표시

==================================================
[8. 폴더 삭제]
==================================================

휴지통 아이콘 클릭 시 삭제 확인 UI를 표시한다.

안내 문구:

"이 폴더를 삭제하면 안의 링크는 '폴더 없음'으로 이동합니다."

중요:

폴더를 삭제한다고 해서 user_bookmarks 자체를 삭제하면 안 된다.

삭제는 반드시 다음 순서로 진행한다.

1.  현재 사용자에게 속하고 해당 folder_id를 가진 user_bookmarks의
    folder_id를 null로 UPDATE

2.  그 다음 현재 사용자 소유의 folders 행을 DELETE

즉:

user_bookmarks
folder_id → null

후

folders
해당 행 삭제

이 순서를 반드시 지킨다.

첫 번째 작업이 실패하면 두 번째 작업을 실행하지 않는다.

두 번째 작업이 실패하더라도 사용자 북마크 자체는 삭제되지 않아야 한다.

성공하면:

- folders 목록에서 삭제된 폴더 제거
- activeFolderFilter를 "all"로 변경
- 북마크 목록에서 해당 링크들이 "폴더 없음" 상태로 반영
- 성공 토스트 표시

==================================================
[9. 북마크를 폴더에 넣기]
==================================================

바로가기의 별표를 클릭해서 새 북마크를 만들 때는
폴더를 선택하는 UI를 띄우지 않는다.

기존 동작 그대로:

별표 클릭
→ user_bookmarks INSERT
→ folder_id: null

즉, 북마크 생성은 계속 원클릭으로 유지한다.

폴더 지정은 이미 저장된 북마크를
북마크 섹션에서 관리할 때 한다.

==================================================
[10. 북마크 폴더 변경]
==================================================

BookmarkedLinkRow에 작은 <select>를 추가한다.

스타일은 기존 Programs 필터의 select 스타일을 최대한 재사용한다.

옵션:

- 폴더 없음
- 현재 사용자가 만든 폴더 목록

사용자가 폴더를 변경하면:

user_bookmarks UPDATE

조건:

- 현재 user.id
- 해당 item_id

변경 값:

- 선택된 folder_id
- "폴더 없음"이면 null

성공하면 화면 상태도 즉시 업데이트한다.

DB의 기존 RLS가 다른 사용자의 폴더 ID를 지정하는 것을 차단하고 있으므로
프론트에서도 folders 조회 결과를 옵션으로 사용한다.

==================================================
[11. 비로그인 상태]
==================================================

비로그인 사용자는 folders/user_bookmarks 관련 쿼리를 실행하지 않는다.

북마크 섹션은 폴더 칩이나 빈 관리 UI를 보여주는 대신
기존 빈 상태 카드와 비슷한 톤으로 로그인 유도 UI를 표시한다.

문구 예:

"로그인하면 나만의 폴더를 만들 수 있어요."

로그인 버튼을 제공한다.

기존 LinkCard의 별표를 비로그인 상태에서 클릭했을 때
/ login으로 이동하는 기존 동작도 그대로 유지한다.

로그아웃하면 즉시:

- folders = []
- user_bookmarks/linkBookmarks = []
- activeFolderFilter = "all"

로 초기화해서 이전 사용자의 데이터가 화면에 남지 않도록 한다.

==================================================
[12. 기존 기능 보호]
==================================================

이번 작업에서 다음 기능은 건드리지 않는다.

- programData
- scheduleBookmarks
- STORAGE_KEYS.schedule
- "내 일정" 관련 UI
- "내 일정" 관련 localStorage
- 바로가기의 category 필터
- 기존 로그인/회원가입 구조
- Supabase 클라이언트 구조

새 Supabase 클라이언트를 만들지 말고
현재 js/lib/supabase.js의 supabase 클라이언트를 그대로 사용한다.

==================================================
[13. 보안]
==================================================

프론트엔드에서도 모든 사용자 소유 데이터 작업에
현재 로그인 사용자의 user.id를 명시적으로 사용한다.

하지만 RLS를 우회하기 위한 별도의 로직은 만들지 않는다.

특히:

- folders INSERT → user.id
- folders UPDATE → user.id + folder id
- folders DELETE → user.id + folder id
- user_bookmarks UPDATE → user.id + item_id

를 사용한다.

DB의 RLS가 최종 보안 경계를 담당한다.

==================================================
[14. 구현 원칙]
==================================================

기존 HomePage.jsx의 디자인과 구조를 최대한 유지한다.

새로운 디자인 시스템을 만들거나 전체 페이지를 리팩터링하지 않는다.

기존 컴포넌트를 재사용할 수 있으면 재사용한다.

다만 FolderModal 등으로 인해 HomePage.jsx가 지나치게 복잡해진다면
작은 컴포넌트로 분리하는 것은 허용한다.

불필요한 리팩터링은 하지 않는다.

==================================================
[15. 구현 후 실제 검증]
==================================================

코드 수정만 하고 끝내지 말고 실제 브라우저 + 실제 Supabase 프로젝트에서
다음 테스트를 수행한다.

1. 비로그인 상태에서 북마크 섹션 확인
2. 로그인 상태에서 내 폴더만 조회되는지 확인
3. 새 폴더 생성
4. 생성한 폴더 이름 변경
5. 기존 북마크를 폴더에 넣기
6. 폴더 필터 선택 시 해당 폴더 링크만 표시
7. "폴더 없음" 선택 시 folder_id === null인 링크만 표시
8. 폴더 삭제
9. 폴더 삭제 후 해당 북마크가 삭제되지 않고 "폴더 없음"으로 남는지 확인
10. 페이지 새로고침 후 폴더/북마크 상태 복원
11. 로그아웃 후 이전 사용자의 폴더/북마크가 화면에 남지 않는지 확인
12. 다른 사용자의 폴더/북마크가 보이지 않는지 확인
13. 바로가기의 기존 카테고리 필터 정상 작동 확인
14. 기존 "내 일정" 기능 정상 작동 확인
15. 북마크 추가/삭제 기능 정상 작동 확인
16. npm run build 성공 확인
17. 브라우저 콘솔 에러 확인
18. 네트워크 요청에서 4xx/5xx 에러가 없는지 확인

특히 아래 세 가지를 중요하게 검증한다.

- 폴더 삭제 시 북마크 자체가 삭제되지 않는가
- 로그아웃 후 이전 사용자의 데이터가 남지 않는가
- 다른 사용자의 폴더/북마크에 접근할 수 없는가

테스트용 계정이나 테스트 데이터가 필요하다면
기존 QA 방식처럼 테스트 후 반드시 전부 정리하고,
최종 DB 상태까지 확인한다.

==================================================
[16. 최종 보고 형식]
==================================================

작업이 끝나면 다음 내용을 보고해줘.

1. 수정한 파일
2. 각 파일의 변경 내용
3. 추가된 폴더 기능
4. 실제 실행한 Supabase 쿼리
5. 브라우저에서 수행한 테스트 목록과 결과
6. npm run build 결과
7. 콘솔/네트워크 에러 여부
8. 테스트 데이터 정리 여부
9. DB 스키마/RLS 변경 여부
10. 추가로 발견된 문제나 다음 작업이 필요한 부분

중요:
DB 스키마나 RLS를 수정하지 않는 것이 이번 작업의 전제다.
필요하다고 판단되는 변경이 발견되면 임의로 실행하지 말고 먼저 보고해줘.
