다음 작업은 현재까지 구현된 북마크/폴더 기능의 안정성 점검 및 마무리 작업으로 진행해줘.

이번 단계에서는 새로운 기능을 추가하기보다,
현재 구현이 실제 사용자 입장에서 자연스럽게 동작하는지 점검하고
발견되는 작은 UX/검증 문제만 최소한으로 수정한다.

DB 스키마와 RLS는 임의로 변경하지 않는다.

==================================================
[1. 먼저 현재 구현 상태 점검]
==================================================

현재까지 구현된 기능은 다음과 같다.

- bookmark_items → Supabase 조회
- 바로가기 카테고리 필터
- 로그인 사용자별 user_bookmarks
- 북마크 추가/삭제
- folders 조회
- 폴더 생성
- 폴더 이름 변경
- 폴더 삭제
- 북마크의 folder_id 변경
- 전체 / 폴더 없음 / 특정 폴더 필터
- 비로그인 로그인 유도
- 로그아웃 시 사용자 데이터 초기화

이 기능들을 먼저 현재 코드 기준으로 다시 확인한다.

특히 HomePage.jsx에서 상태 간 의존관계와
useEffect 실행 순서를 확인해서 다음 문제가 없는지 점검한다.

- 로그인 직후 폴더와 북마크가 제대로 동시에 로드되는가
- 로그아웃 직후 이전 사용자의 데이터가 남지 않는가
- 사용자를 A → B로 전환했을 때 A의 데이터가 B에게 보이지 않는가
- activeFolderFilter가 삭제된 폴더를 계속 가리키는 경우가 없는가
- 폴더 생성/이름변경 후 현재 선택 상태가 꼬이지 않는가
- 북마크를 삭제한 뒤 폴더 필터 상태가 이상해지지 않는가
- 새로고침 후 DB 상태와 UI 상태가 정확히 일치하는가

문제가 없다면 불필요하게 코드를 수정하지 않는다.

==================================================
[2. 폴더 이름 입력 검증 개선]
==================================================

현재 folders.name에는 DB CHECK constraint가 없고,
프론트엔드 검증이 사실상 유일한 입력 검증이라는 점을 확인했다.

현재 FolderModal의 폴더명 입력 검증을 다시 확인한다.

최소한 다음 조건을 만족하도록 한다.

- 빈 문자열 입력 방지
- 공백만 입력한 경우 방지
- 저장 시 trim()
- 최대 30자 제한

30자 제한을 적용할 경우 사용자에게도 자연스럽게 표시한다.

예:

"폴더 이름은 30자 이내로 입력해주세요."

이미 동일한 검증이 구현되어 있다면 중복 수정하지 않는다.

중요:
이번 단계에서는 DB CHECK constraint를 추가하지 않는다.
프론트엔드 검증만 정리한다.

==================================================
[3. 폴더 이름 변경 UX 확인]
==================================================

폴더 이름 변경 시 다음을 확인한다.

- 기존 이름이 input에 정확하게 들어오는가
- 저장하지 않고 취소하면 기존 이름이 그대로 유지되는가
- 빈 이름으로 변경할 수 없는가
- 이름 변경 성공 후 폴더 칩과 현재 선택 영역이 즉시 갱신되는가
- 모달을 닫았다가 다시 열었을 때 이전 입력값이 남지 않는가

==================================================
[4. 폴더 삭제 UX 확인]
==================================================

현재 삭제 로직은 다음 순서를 반드시 유지한다.

1. 해당 폴더의 user_bookmarks.folder_id → null
2. folders에서 해당 폴더 삭제

이 순서는 변경하지 않는다.

삭제 확인 모달에서 사용자가 취소하면 아무 데이터도 변경하지 않는지 확인한다.

삭제 성공 후:

- 폴더 칩 제거
- activeFolderFilter → "all"
- 기존 북마크는 그대로 유지
- 해당 북마크의 폴더 select → "폴더 없음"
- 북마크 개수도 그대로 유지

가 되는지 확인한다.

==================================================
[5. 폴더 필터 UX 확인]
==================================================

다음 상태를 실제 브라우저에서 확인한다.

전체
→ 모든 내 북마크 표시

폴더 없음
→ folder_id === null인 북마크만 표시

특정 폴더
→ 해당 folder_id의 북마크만 표시

그리고 폴더가 하나도 없는 사용자도 UI가 깨지지 않는지 확인한다.

북마크는 있지만 폴더가 없는 경우:

[전체] [폴더 없음] [+ 새 폴더]

형태로 자연스럽게 표시되는지 확인한다.

폴더는 있지만 북마크가 하나도 없는 경우에도
빈 상태 UI가 자연스럽게 표시되는지 확인한다.

==================================================
[6. BookmarkedLinkRow 확인]
==================================================

폴더 select 추가 이후 모바일에서 레이아웃이 깨지지 않는지 확인한다.

특히:

- 긴 제목
- 긴 폴더명
- 폴더가 여러 개일 때
- 폴더 없음
- 모바일 화면
- 데스크톱 화면

에서 overflow나 잘림이 없는지 확인한다.

기존 디자인을 크게 바꾸지는 않는다.

필요한 경우에만 최소한의 CSS 수정으로 해결한다.

==================================================
[7. 로딩/에러 상태 확인]
==================================================

다음 상태가 사용자에게 자연스럽게 표시되는지 확인한다.

- bookmark_items 로딩
- folders 로딩
- bookmark_items 조회 실패
- folders 조회 실패
- 폴더 생성 실패
- 폴더 이름 변경 실패
- 폴더 삭제 실패
- bookmark folder 변경 실패

특히 Supabase 요청 실패 시 UI 상태가 성공한 것처럼 남지 않는지 확인한다.

낙관적 업데이트를 사용하는 부분은 실패 시 롤백되는지 확인한다.

==================================================
[8. 권한/보안 회귀 테스트]
==================================================

기존 RLS는 변경하지 않는다.

실제 Supabase 프로젝트에서 최소한 다음을 다시 확인한다.

- user A가 user B의 folders를 조회할 수 없는가
- user A가 user B의 folder_id를 자신의 bookmark에 지정할 수 없는가
- user A가 user B의 bookmark를 조회할 수 없는가
- user A가 user B의 bookmark를 수정/삭제할 수 없는가

테스트 계정과 데이터는 테스트 후 반드시 삭제한다.

최종적으로 테스트 데이터가 남아있지 않은지 확인한다.

==================================================
[9. 기존 기능 회귀 테스트]
==================================================

폴더 기능 때문에 기존 기능이 깨지지 않았는지 확인한다.

반드시 확인:

- 바로가기 전체 목록
- 공식/예매/SNS/여행/교통/미디어/참여 필터
- 바로가기 북마크 추가
- 바로가기 북마크 삭제
- 페이지 새로고침 후 북마크 유지
- 비로그인 상태에서 별표 클릭 → /login
- 로그인/로그아웃
- "내 일정"
- 기존 프로그램 북마크
- 공유 모달
- 기존 페이지 이동

특히 "내 일정" 관련 코드와 데이터는 수정하지 않는다.

==================================================
[10. 코드 정리]
==================================================

기능상 문제가 없는 범위에서만 HomePage.jsx를 점검한다.

다음과 같은 불필요한 코드가 남아 있다면 제거한다.

- 더 이상 사용하지 않는 import
- 더 이상 사용하지 않는 상태
- dead code
- 이전 localStorage 북마크 관련 잔여 코드
- 사용되지 않는 함수

단, 이번 단계에서 대규모 리팩터링은 하지 않는다.

현재 코드 구조를 유지한다.

==================================================
[11. 테스트]
==================================================

수정 후 반드시 실제 브라우저에서 테스트한다.

최소 테스트:

1. 비로그인 화면
2. 로그인
3. 폴더 생성
4. 폴더 이름 변경
5. 북마크를 폴더에 지정
6. 폴더 필터
7. 폴더 없음 필터
8. 폴더 삭제
9. 삭제 후 북마크 보존
10. 새로고침
11. 로그아웃
12. 다른 사용자 접근 차단
13. 기존 바로가기 필터
14. 기존 북마크 추가/삭제
15. 내 일정
16. 모바일 레이아웃
17. npm run build
18. 콘솔 에러
19. 네트워크 4xx/5xx

==================================================
[12. 중요한 제한]
==================================================

이번 작업에서는 다음을 하지 않는다.

- 새로운 DB migration 생성
- RLS 정책 변경
- FK 변경
- CHECK constraint 추가
- categories 테이블 추가
- 새로운 Supabase 클라이언트 생성
- "내 일정" 기능 리팩터링
- 전체 HomePage 구조를 대규모로 리팩터링

DB 변경이 필요하다고 판단되는 문제가 발견되면
임의로 실행하지 말고 먼저 보고한다.

==================================================
[13. 최종 보고]
==================================================

작업이 끝나면 다음 형식으로 보고해줘.

1. 점검한 파일
2. 실제 수정한 부분
3. 수정하지 않은 이유가 있는 부분
4. 폴더 이름 검증 상태
5. 폴더 생성/수정/삭제 테스트 결과
6. 폴더 필터 테스트 결과
7. 권한/RLS 테스트 결과
8. 기존 기능 회귀 테스트 결과
9. 모바일/데스크톱 UI 확인 결과
10. npm run build 결과
11. 콘솔/네트워크 에러 여부
12. 테스트 데이터 정리 여부
13. DB/RLS 변경 여부
14. 추가로 발견된 문제
15. 다음 작업을 추천한다면 무엇인지

문제가 발견되지 않았다면 억지로 수정하지 말고
"문제 없음"이라고 명확하게 보고해줘.
