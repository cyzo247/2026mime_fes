이제 Supabase DB의 RLS(Row Level Security)를 설계할 단계야.

현재 프로젝트의 데이터 구조는 다음과 같아.

1. bookmark_items

- 서비스가 제공하는 북마크 카탈로그
- 특정 사용자에게 귀속되지 않음
- 모든 사용자가 동일한 목록을 봄
- 사용자가 직접 생성하는 데이터가 아님

2. folders

- 사용자가 자신의 북마크를 분류하기 위한 폴더
- user_id가 auth.users.id를 참조함
- 폴더는 사용자 개인 데이터임

3. user_bookmarks

- 어떤 사용자가 어떤 bookmark_item을 담았는지 기록
- user_id가 auth.users.id를 참조함
- item_id가 bookmark_items.id를 참조함
- folder_id는 folders.id를 참조하며 NULL 가능
- (user_id, item_id)는 UNIQUE

현재 세 테이블의 RLS는 모두 꺼져 있음.

중요한 점:

- 아직 RLS를 실제로 활성화하거나 정책을 생성하지 마.
- 먼저 현재 코드와 DB 구조를 분석해서 필요한 RLS 정책을 설계하고 설명해줘.
- 코드나 SQL을 수정하지 말고 분석만 해줘.

다음 관점에서 검토해줘.

1. bookmark_items

- 비로그인 사용자도 읽을 수 있어야 하는지
- 로그인 사용자가 읽을 수 있어야 하는지
- 일반 사용자가 INSERT/UPDATE/DELETE할 수 없어야 하는지

2. folders

- 로그인한 사용자가 자신의 폴더만 SELECT할 수 있어야 하는지
- 자신의 폴더만 INSERT할 수 있어야 하는지
- 자신의 폴더만 UPDATE/DELETE할 수 있어야 하는지
- 다른 사용자의 폴더를 user_id만 바꿔서 조작할 수 없는지

3. user_bookmarks

- 로그인한 사용자가 자신의 북마크 기록만 SELECT할 수 있어야 하는지
- 자신의 북마크만 INSERT/UPDATE/DELETE할 수 있어야 하는지
- 다른 사용자의 user_bookmarks를 조회하거나 수정할 수 없는지
- folder_id를 다른 사용자의 폴더로 지정하는 것이 가능한지 여부도 검토해줘

특히 user_bookmarks에서
"내가 가진 북마크 → 내가 가진 폴더"
라는 관계까지 RLS 정책으로 안전하게 보장할 수 있는지 검토해줘.

현재 코드에서 Supabase 쿼리가 어떻게 작성되어 있는지도 확인해서,
앞으로 RLS를 적용했을 때 코드 수정이 필요한 부분이 있는지도 함께 알려줘.

최종적으로 다음 형식으로 보고해줘.

- 테이블별 RLS 활성화 여부
- 필요한 SELECT / INSERT / UPDATE / DELETE 정책
- 각 정책의 조건
- 정책을 적용했을 때 기존 코드에서 수정해야 할 부분
- 정책 적용 전에 주의해야 할 사항

이번 단계에서는 절대 DB를 수정하지 마.
