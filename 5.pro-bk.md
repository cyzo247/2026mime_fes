좋아. 지금까지 완료한 북마크/폴더 기능은 그대로 유지하고, 이번 단계에서는 "내 일정" 기능의 현재 구조를 분석만 해줘.

중요:

- 이번 단계에서는 코드와 DB를 절대 수정하지 마.
- migration 생성/실행 금지.
- INSERT/UPDATE/DELETE 금지.
- 기존 북마크, 폴더, user_bookmarks, folders 기능도 수정하지 마.
- 분석 결과만 보고해줘.
- 현재 코드에 있는 내용을 추측해서 보완하지 말고 실제 파일/DB 구조를 확인해서 보고해줘.

목표는 현재 HomePage의 "내 일정" 기능을 나중에 Supabase 기반으로 전환할 수 있는지 판단하는 것이다.

다음 순서대로 조사해줘.

1. 현재 "내 일정" 데이터의 출처 확인

- js/pages/HomePage.jsx에서 programData가 어떻게 정의되어 있는지 확인
- 프로그램 전체 목록이 하드코딩인지, 별도 파일인지, API/DB에서 가져오는지 확인
- programData의 전체 항목 수와 각 항목의 실제 필드 구조를 보고해줘.
- 현재 localStorage를 사용하는 부분이 있다면 STORAGE_KEYS.schedule, scheduleBookmarks 등을 포함해서 정확히 확인해줘.

2. "내 일정" UI 동작 분석

- 프로그램 목록이 화면에서 어떻게 렌더링되는지
- 일정 추가/삭제가 어떻게 동작하는지
- 필터/날짜/카테고리/시간 등의 UI가 있는지
- ShareModal 등 다른 기능과 어떤 관계가 있는지
- 새로고침했을 때 일정 데이터가 어떻게 유지되는지
- 비로그인 상태에서 어떤 동작을 하는지

3. 현재 데이터 구조 정리
   programData의 각 필드를 표로 정리해줘.

예:
필드 | 타입 | 현재 값 예시 | 용도 | DB로 옮길 필요 여부

특히 다음과 같은 정보가 있는지 확인해줘.

- id
- 날짜
- 시작/종료 시간
- 프로그램명
- 설명
- 장소
- 장르/카테고리
- 출연자/아티스트
- 이미지
- 기타 필드

실제 코드에 존재하는 필드만 보고해줘.

4. Supabase 현재 구조와 비교
   현재 Supabase에 이미 존재하는 테이블 중 "내 일정"과 관련될 수 있는 것이 있는지 확인해줘.

특히:

- bookmark_items
- user_bookmarks
- folders
- 기타 관련 테이블

그리고 현재 스키마만으로 "내 일정"을 저장할 수 있는지, 아니면 별도의 프로그램 테이블이 필요한지 판단해줘.

단, 이번 단계에서는 스키마를 실제로 변경하지 마.

5. 기존 북마크 시스템과의 관계
   중요하게 확인해줘.

현재 구조에서:

- 바로가기 북마크(user_bookmarks)
- 내 일정(scheduleBookmarks)

이 둘이 완전히 별개의 데이터인지,
혹은 서로 연결되어 있는 부분이 있는지 확인해줘.

특히 "프로그램을 일정에 추가하는 것"과 "바로가기 링크를 북마크하는 것"이 코드상 어떻게 분리되어 있는지 확인해줘.

6. localStorage 의존성
   내 일정 기능에서 localStorage가 정확히 어디까지 사용되는지 조사해줘.

- 저장 key
- 저장되는 데이터 형태
- 읽는 위치
- 쓰는 위치
- 삭제하는 위치
- 로그인 여부와 관계가 있는지

그리고 Supabase로 이전할 경우 어떤 부분을 교체해야 하는지 "설계 수준"에서만 제안해줘.

7. 보안/RLS 관점
   현재 내 일정이 개인 데이터라면 Supabase에서 어떤 형태로 저장해야 하는지 검토해줘.

다만 새로운 RLS 정책이나 migration은 만들지 마.

현재 존재하는 RLS 정책으로 재사용 가능한 부분이 있는지, 아니면 별도 정책이 필요한지 판단만 해줘.

8. 데이터 이관 난이도
   현재 programData를 Supabase로 옮긴다고 가정했을 때:

- 그대로 옮길 수 있는 데이터
- 새로 정해야 하는 데이터
- DB에 새 컬럼이 필요한 데이터
- 현재 코드에서 버려도 되는 데이터
- 사용자별 데이터와 공용 데이터 중 무엇인지

를 구분해줘.

9. 구현 순서 제안
   마지막에는 실제 구현을 한다면 어떤 순서로 진행하는 게 가장 안전한지 제안해줘.

예:
① 현재 구조 분석
② DB 스키마 설계
③ migration
④ 데이터 이관
⑤ 프론트 조회 연결
⑥ 사용자 일정 저장 연결
⑦ 기존 localStorage 제거
⑧ QA

단, 이번 단계에서는 ①에 해당하는 분석까지만 진행하는 것으로 해줘.

10. 회귀 테스트 범위
    다음 단계에서 실제 구현할 경우 반드시 기존 기능이 깨지지 않았는지 확인해야 할 항목도 정리해줘.

특히 다음 기능들은 절대 깨지면 안 됨:

- 바로가기 10개 표시
- 카테고리 필터
- 로그인/로그아웃
- 북마크 추가/삭제
- 북마크 새로고침 유지
- 폴더 생성
- 폴더 이름 변경
- 폴더 삭제
- 폴더별 필터
- 북마크의 폴더 지정/변경
- 다른 사용자 데이터 격리
- ShareModal
- 현재 "내 일정" 기능

최종 보고서는 다음 형식으로 작성해줘.

1. 현재 내 일정 데이터 구조
2. 현재 UI/동작 구조
3. localStorage 사용 구조
4. Supabase 현재 구조와 비교
5. 바로가기 북마크와의 관계
6. DB로 이전할 때 필요한 변경점
7. 예상되는 migration/스키마 방향
8. 구현 순서
9. 회귀 테스트 체크리스트
10. 발견된 문제/주의사항

다시 강조하지만 이번 작업에서는 코드와 DB를 수정하지 말고 분석과 설계만 해줘.
