좋아. 이제부터는 실제 구현 단계로 진행해줘.

이번 작업의 최종 목표는 현재 "내 일정" 기능을 Supabase 기반으로 전환하고, 기존 기능을 모두 유지한 상태에서 프로젝트를 배포 가능한 수준까지 완성하는 것이다.

시간이 빠듯하므로 불필요하게 작업을 쪼개거나 다시 설계만 하지 말고, 아래 순서를 따라 실제 구현 → 검증까지 진행해줘.

단, 안전을 위해 반드시 현재 분석 결과에서 확인된 실제 코드/DB 구조를 기준으로 작업하고, 존재하지 않는 필드나 파일을 추측해서 만들지 마.

━━━━━━━━━━━━━━━━━━
[1. DB 스키마 설계 및 migration]
━━━━━━━━━━━━━━━━━━

현재 "내 일정" 분석 결과를 기준으로 Supabase에 필요한 테이블/컬럼을 결정해줘.

원칙:

- 공용 프로그램 데이터와 사용자 개인 일정 데이터를 구분할 것.
- 프로그램 자체는 여러 사용자가 공유하는 데이터이므로 공용 테이블로 설계.
- 사용자가 어떤 프로그램을 자신의 일정에 추가했는지는 사용자별 데이터로 분리.
- 기존 bookmark_items / user_bookmarks / folders 구조와 의미가 충돌하지 않도록 할 것.
- 기존 테이블/RLS/FK/UNIQUE는 최대한 변경하지 말 것.
- 이미 존재하는 구조를 재사용할 수 있다면 새 구조를 중복해서 만들지 말 것.
- 프로그램 데이터에 실제로 필요한 컬럼만 만들 것.
- 현재 코드에 없는 정보를 임의로 추가하지 말 것.

필요하다면 새로운 migration 파일을 생성해도 된다.

migration에는 다음이 포함되어야 한다.

- 필요한 테이블 생성
- PRIMARY KEY
- 필요한 FK
- 필요한 UNIQUE
- 필요한 NOT NULL
- 필요한 기본값
- 사용자 개인 데이터에 필요한 RLS
- 공용 프로그램 데이터에 필요한 SELECT 정책

RLS는 반드시 실제 사용자를 기준으로 설계해줘.
다른 사용자의 일정 데이터가 조회/수정/삭제되지 않도록 DB 레벨에서 막아야 한다.

migration 작성 후 반드시 실제 Supabase 프로젝트에 push하고,
적용 결과를 SQL로 확인해줘.

━━━━━━━━━━━━━━━━━━
[2. 기존 programData 이관]
━━━━━━━━━━━━━━━━━━

현재 HomePage.jsx 또는 실제 데이터 파일에 존재하는 프로그램 데이터를 새 프로그램 테이블로 이관해줘.

주의:

- 현재 데이터의 제목/날짜/시간/장소/설명/카테고리 등 실제 존재하는 값은 그대로 이관.
- 데이터 내용을 임의로 수정하지 말 것.
- 기존 programData의 id가 DB identity와 타입이 다르다면 적절한 방식으로 처리.
- 기존 프로그램 데이터의 순서가 UI에 중요하다면 그 순서가 유지되도록 할 것.
- 중복 데이터가 생기지 않도록 사전/사후 count 및 unique 검증을 할 것.

이관 후 실제 DB에서 전체 프로그램 수와 주요 필드를 확인해줘.

━━━━━━━━━━━━━━━━━━
[3. HomePage를 Supabase 기반으로 전환]
━━━━━━━━━━━━━━━━━━

현재 "내 일정"이 programData/localStorage를 기반으로 동작한다면,
공용 프로그램 목록은 Supabase에서 SELECT하도록 변경해줘.

기존 UI 디자인과 동작은 최대한 그대로 유지한다.

특히:

- 프로그램 목록 표시
- 날짜/시간 표시
- 프로그램 필터
- 일정 추가/삭제
- ShareModal
- 기존 버튼/아이콘
- 빈 상태 UI

등은 현재 디자인을 유지한다.

프로그램 목록에 로딩/에러 상태가 필요하다면
현재 바로가기/폴더에서 사용한 패턴을 재사용해줘.

새로운 Supabase client를 만들지 말고,
기존 js/lib/supabase.js의 클라이언트를 재사용해줘.

━━━━━━━━━━━━━━━━━━
[4. 내 일정 저장 방식 변경]
━━━━━━━━━━━━━━━━━━

기존 scheduleBookmarks/localStorage 기반 저장을
사용자별 Supabase 데이터로 전환해줘.

로그인 사용자:

- 프로그램을 일정에 추가하면 Supabase INSERT
- 일정에서 제거하면 Supabase DELETE
- 새로고침하면 Supabase에서 다시 조회
- 다른 브라우저/기기에서도 동일한 계정으로 로그인하면 일정이 유지되어야 함

비로그인 사용자:

- 개인 일정 데이터를 Supabase에 저장하지 말 것.
- 현재 UX를 최대한 유지하되, 로그인이 필요한 기능이라면 기존 프로젝트의 로그인 유도 방식과 일관되게 처리할 것.

개인 일정 데이터에는 반드시 현재 로그인 사용자의 user.id를 사용해줘.

프론트에서 user_id를 임의로 만들거나 localStorage에 사용자 식별자를 저장하지 말 것.

━━━━━━━━━━━━━━━━━━
[5. 낙관적 업데이트 및 오류 처리]
━━━━━━━━━━━━━━━━━━

현재 북마크/폴더 기능에서 사용한 패턴을 참고해서
내 일정 추가/삭제도 가능하면 낙관적 업데이트를 적용해줘.

단:

- INSERT 실패 → 화면 상태 롤백
- DELETE 실패 → 화면 상태 롤백
- 사용자에게 에러 토스트 표시

네트워크 오류가 발생했을 때 화면과 DB 상태가 서로 다른 상태로 남지 않도록 처리해줘.

━━━━━━━━━━━━━━━━━━
[6. 기존 localStorage 일정 데이터 처리]
━━━━━━━━━━━━━━━━━━

기존 scheduleBookmarks가 localStorage에 저장되고 있다면,
Supabase 전환이 완료된 후 더 이상 새로운 일정 데이터를 localStorage에 저장하지 않도록 해줘.

단, 사용자의 기존 localStorage 데이터가 실제 서비스에서 존재할 가능성이 있다면 바로 삭제하지 말고 먼저 분석해줘.

기존 데이터의 자동 마이그레이션이 필요하다고 판단되면,
현재 프로젝트 구조에서 가장 안전한 방법으로 처리해줘.

반대로 기존 localStorage 데이터가 단순 테스트 데이터이거나 실제 사용자 데이터가 아니라고 확인된다면 제거해도 된다.

이 부분은 추측하지 말고 실제 코드를 확인해서 판단해줘.

━━━━━━━━━━━━━━━━━━
[7. 기존 기능 절대 회귀시키지 말 것]
━━━━━━━━━━━━━━━━━━

이번 작업에서 다음 기능은 반드시 그대로 유지되어야 한다.

① 바로가기

- Supabase bookmark_items에서 10개 표시
- 카테고리 필터
- 링크 클릭
- 카드 UI

② 개인 북마크

- 로그인 사용자 북마크 추가
- 북마크 삭제
- 새로고침 후 유지
- 비로그인 사용자는 /login 이동

③ 폴더

- 폴더 생성
- 폴더 이름 변경
- 폴더 삭제
- 삭제 시 북마크는 유지되고 folder_id만 NULL
- 폴더별 필터
- "폴더 없음" 필터
- 북마크의 폴더 지정/변경
- 사용자별 데이터 격리

④ 인증

- 로그인
- 로그아웃
- 로그인 상태 변화에 따른 데이터 초기화/조회

⑤ 내 일정

- 이번 작업으로 Supabase 기반 전환
- 기존 UI/동작 유지

⑥ ShareModal

- 기존 일정 공유 기능이 있다면 반드시 정상 동작 유지

━━━━━━━━━━━━━━━━━━
[8. 실제 브라우저 QA]
━━━━━━━━━━━━━━━━━━

구현 후 반드시 실제 브라우저에서 테스트해줘.

가능하면 이전 QA와 동일하게 실제 Supabase 프로젝트를 사용해서 검증한다.

최소 다음을 테스트해줘.

[공용 프로그램]

1. 비로그인 상태에서 프로그램 목록 조회
2. 프로그램 개수 확인
3. 프로그램 필터 동작
4. 기존 UI 정상 표시

[내 일정] 5. 로그인 6. 프로그램 1개 일정 추가 7. DB INSERT 확인 8. 일정 목록 UI 반영 9. page.reload() 10. 일정 상태 복원 11. 프로그램 일정 삭제 12. DB DELETE 확인 13. page.reload() 후 삭제 상태 유지

[사용자 격리] 14. user A가 일정 추가 15. user B로 로그인 16. user A의 일정이 보이지 않는지 확인 17. user B가 user A의 일정 데이터를 직접 조회/수정/삭제할 수 없는지 RLS 확인

[기존 기능 회귀] 18. 바로가기 10개 표시 19. 카테고리 필터 20. 북마크 추가 21. 북마크 삭제 22. 새로고침 후 북마크 유지 23. 폴더 생성 24. 폴더 이름 변경 25. 폴더에 북마크 넣기 26. 폴더 필터 27. 폴더 삭제 후 북마크 보존 28. 비로그인 북마크 클릭 → /login 29. ShareModal 30. 로그아웃 후 개인 데이터 화면 초기화

[기술 검증] 31. npm run build 32. 브라우저 console error 확인 33. 네트워크 4xx/5xx 확인 34. 모바일 375px 화면 확인 35. 데스크톱 화면 확인

━━━━━━━━━━━━━━━━━━
[9. 테스트 데이터 정리]
━━━━━━━━━━━━━━━━━━

QA를 위해 임시 사용자/일정 데이터를 생성했다면
테스트 종료 후 반드시 전부 삭제해줘.

단, 실제 사용자의 데이터는 절대 삭제하지 말 것.

테스트 계정은 반드시 명확한 UUID 또는 테스트용 식별자로 스코프해서 정리해줘.

최종적으로 다음을 확인해줘.

- 테스트 auth.users = 0
- 테스트 folders = 0
- 테스트 user_bookmarks = 0
- 테스트 일정 데이터 = 0

단, 기존 실제 사용자 데이터는 건드리지 않았음을 확인해줘.

━━━━━━━━━━━━━━━━━━
[10. 최종 코드 정리]
━━━━━━━━━━━━━━━━━━

기능 구현과 QA가 모두 통과한 뒤에만 정리 작업을 해줘.

확인할 것:

- 사용하지 않는 import
- 사용하지 않는 state
- 사용하지 않는 effect
- dead code
- 기존 localStorage 일정 저장 코드
- console.log
- TODO
- 임시 테스트 코드
- 불필요한 주석

단, 기존 프로젝트에서 의도적으로 사용하는 코드는 삭제하지 말 것.

━━━━━━━━━━━━━━━━━━
[11. 최종 보고서]
━━━━━━━━━━━━━━━━━━

작업이 끝나면 다음 형식으로 보고해줘.

1. 생성/수정된 파일
2. 생성된 migration
3. 최종 DB 테이블 구조
4. RLS 정책
5. 기존 programData 이관 결과
6. 내 일정 Supabase 전환 결과
7. localStorage 처리 결과
8. 기존 북마크/폴더 기능 회귀 테스트 결과
9. 모바일/데스크톱 테스트 결과
10. npm run build 결과
11. console/network 에러 여부
12. 테스트 데이터 정리 결과
13. 발견된 문제
14. 아직 남은 작업
15. 현재 프로젝트 완성도를 0~100%로 평가

중요:
이번 작업은 "분석만" 하는 단계가 아니다.
필요한 DB migration, 데이터 이관, 프론트엔드 수정, QA까지 실제로 진행해줘.

단, 기존 기능을 깨뜨리는 변경이나 불필요한 대규모 리팩터링은 하지 말고,
현재 구조를 최대한 유지하면서 필요한 부분만 수정해줘.

그리고 작업 도중 문제가 발견되면 임의로 무시하지 말고,
가능한 안전한 범위에서 직접 수정한 뒤 다시 테스트해줘.

최종 목표는 "내 일정까지 Supabase 기반으로 정상 작동하고, 기존 북마크/폴더/인증 기능까지 모두 정상인 상태"다.
