지금까지 만든 Supabase 데이터베이스 구조를 최종적으로 점검해줘.

현재 테이블은 다음 3개다.

1. bookmark_items
2. folders
3. user_bookmarks

다음 관계가 정확하게 설정되어 있는지 확인해줘.

- user_bookmarks.item_id → bookmark_items.id
- user_bookmarks.folder_id → folders.id
- user_bookmarks.user_id → Supabase Auth 사용자
- folders.user_id → Supabase Auth 사용자

그리고 다음 조건도 확인해줘.

- bookmark_items는 서비스에서 제공하는 원본 북마크 항목이다.
- 여러 사용자가 하나의 bookmark_item을 자신의 북마크로 추가할 수 있다.
- 사용자가 북마크를 삭제해도 bookmark_items는 삭제되지 않는다.
- 같은 사용자가 같은 bookmark_item을 중복해서 추가할 수 없다.
- 폴더는 사용자별로 관리된다.
- 현재 모든 테이블의 RLS는 꺼져 있다.

각 테이블의 컬럼과 관계를 표 형태로 정리해서 보여줘.

이 단계에서는 데이터베이스를 수정하지 말고,
현재 상태를 점검하고 문제가 있는 부분이 있다면 한국말로 먼저 알려줘.
