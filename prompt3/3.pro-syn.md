이제 기존 로그인/회원가입/로그아웃 목업을 실제 Supabase Auth와 연결해줘.

현재 프로젝트에는 이미 다음이 준비되어 있어:

- React + Vite
- Supabase 프로젝트 및 DB 스키마
- bookmark_items, folders, user_bookmarks 테이블
- folders.user_id → auth.users.id FK
- user_bookmarks.user_id → auth.users.id FK
- RLS는 현재 꺼져 있음
- .env.local에 VITE_SUPABASE_URL과 VITE_SUPABASE_ANON_KEY가 설정되어 있음

.env.local:
VITE_SUPABASE_URL=...
VITE_SUPABASE_ANON_KEY=...

다음 작업을 진행해줘.

1. @supabase/supabase-js가 설치되어 있는지 확인하고, 없다면 설치해줘.

2. Supabase 클라이언트를 생성하는 파일을 추가해줘.
   예: js/lib/supabase.js

    환경변수는 import.meta.env.VITE_SUPABASE_URL
    import.meta.env.VITE_SUPABASE_ANON_KEY
    를 사용해줘.

3. 기존 AuthContext를 실제 Supabase Auth 기반으로 변경해줘.
    - supabase.auth.getSession()
    - supabase.auth.onAuthStateChange()
      를 사용해서 새로고침해도 실제 Supabase 세션이 유지되도록 해줘.
    - 기존 localStorage 기반의 가짜 인증 상태 관리는 제거해줘.
    - login/logout 함수도 실제 Supabase Auth를 사용하도록 변경해줘.

4. SignupPage를 실제 회원가입으로 변경해줘.
    - 이메일과 비밀번호를 Supabase Auth의 signUp()으로 전달
    - 비밀번호 확인은 기존처럼 프론트엔드에서 검증
    - 회원가입 성공/실패에 대한 사용자 피드백을 표시
    - 성공했다고 해서 무조건 로그인된 것으로 처리하지 말고, Supabase의 실제 인증 상태를 기준으로 처리해줘.
    - 이메일 인증이 필요한 경우 현재 Supabase 설정에 맞게 안내해줘.

5. LoginPage를 실제 로그인으로 변경해줘.
    - 기존 DEMO_ACCOUNT 하드코딩 및 문자열 비교 로직 제거
    - supabase.auth.signInWithPassword() 사용
    - 로그인 실패 시 적절한 에러 메시지 표시
    - 성공하면 기존 앱의 로그인 후 화면으로 이동

6. 로그아웃도 실제 Supabase의 signOut()을 사용하도록 변경해줘.

7. 기존 UI/디자인/페이지 구조는 최대한 유지하고, 인증 기능에 필요한 부분만 수정해줘.

8. 작업 후 다음 내용을 확인해서 보고해줘.
    - 수정/추가한 파일
    - 기존 목업 인증 로직 중 제거한 부분
    - 실제 Supabase Auth를 호출하는 부분
    - 회원가입 → auth.users 생성 여부를 확인하는 방법
    - 로그인 → 세션 생성 및 유지 여부
    - 로그아웃 → 세션 제거 여부

중요:

- DB 테이블이나 FK 구조는 변경하지 마.
- RLS도 변경하지 마.
- .env.local의 실제 키 값은 코드에 하드코딩하지 마.
- 기존 UI를 불필요하게 수정하지 마.
- 먼저 현재 코드를 확인한 뒤 필요한 부분만 수정해줘.
