좋아. 방금 제안한 RLS 설계대로 실제 Supabase DB에 정책을 적용해줘.

적용 대상은 다음 3개 테이블이야.

1. bookmark_items

- RLS 활성화
- SELECT: anon/authenticated 모두 허용
- USING (true)
- INSERT / UPDATE / DELETE 정책은 만들지 않음
- 결과적으로 일반 클라이언트에서는 읽기만 가능하도록 설정

2. folders

- RLS 활성화
- SELECT: auth.uid() = user_id
- INSERT: WITH CHECK (auth.uid() = user_id)
- UPDATE:
    - USING (auth.uid() = user_id)
    - WITH CHECK (auth.uid() = user_id)
- DELETE: USING (auth.uid() = user_id)

3. user_bookmarks

- RLS 활성화
- SELECT: auth.uid() = user_id
- INSERT:
    - auth.uid() = user_id
    - folder_id가 NULL이거나 해당 folder_id가 현재 사용자의 폴더인지 검증
- UPDATE:
    - USING (auth.uid() = user_id)
    - WITH CHECK:
        - auth.uid() = user_id
        - folder_id가 NULL이거나 해당 folder_id가 현재 사용자의 폴더인지 검증
- DELETE: USING (auth.uid() = user_id)

user_bookmarks의 folder_id 소유권 검증은 다음 논리를 사용해줘.

folder_id IS NULL
OR EXISTS (
SELECT 1
FROM public.folders f
WHERE f.id = folder_id
AND f.user_id = auth.uid()
)

중요:

- 기존 컬럼 구조를 변경하지 마.
- 기존 FK를 변경하거나 삭제하지 마.
- 기존 UNIQUE 제약을 변경하지 마.
- 기존 DB 데이터를 삭제하지 마.
- Auth 설정을 변경하지 마.
- .env.local은 수정하지 마.
- RLS 외의 변경은 하지 마.

적용 전에 현재 RLS 상태와 기존 정책을 확인하고,
충돌할 수 있는 기존 정책이 있다면 먼저 보고해줘.

적용 후에는 실제로 다음을 확인해줘.

1. 세 테이블의 RLS가 활성화되었는지
2. 각 테이블에 어떤 정책이 생성되었는지
3. bookmark_items가 비로그인 상태에서 SELECT 가능한지
4. folders가 사용자별로 격리되는 구조인지
5. user_bookmarks가 사용자별로 격리되는 구조인지
6. 다른 사용자의 folder_id를 user_bookmarks에 지정할 수 없는 구조인지
7. 기존 FK와 UNIQUE 제약이 그대로 유지되는지

SQL 실행 및 검증이 끝난 뒤 실제 결과를 자세히 보고해줘.
