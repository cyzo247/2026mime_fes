좋아. 이제 Supabase의 bookmark_items 데이터를 HomePage에 연결하자.

현재 상태:

- bookmark_items 테이블에 초기 데이터 10개가 정상적으로 들어가 있음
- bookmark_items.category 컬럼도 존재함
- bookmark_items RLS가 활성화되어 있고 anon/authenticated의 SELECT가 허용되어 있음
- 현재 HomePage.jsx는 linkData라는 하드코딩 배열을 사용하고 있음
- 현재 UI의 카테고리 필터와 카드 디자인은 정상적으로 구현되어 있음

이번 단계의 목표는
"HomePage의 바로가기 링크 데이터를 하드코딩 데이터에서 Supabase bookmark_items 조회 결과로 교체하는 것"이다.

작업 범위:

1. HomePage.jsx에서 현재 하드코딩되어 있는 linkData 배열을 제거하거나 더 이상 사용하지 않도록 변경해주세요.

2. Supabase의 bookmark_items를 SELECT해서 링크 목록을 가져오도록 구현해주세요.

조회할 컬럼:

- id
- url
- title
- description
- thumbnail_url
- category
- created_at

3. 현재 js/lib/supabase.js에 만들어진 Supabase 클라이언트를 재사용해주세요.
   새로운 Supabase 클라이언트를 만들지 마세요.

4. 기존 UI를 최대한 그대로 유지해주세요.
   특히 다음 기능은 현재와 동일하게 동작해야 합니다.

- 바로가기 링크 카드
- 카테고리 필터
- "전체" 필터
- 링크 클릭
- 기존 카드의 제목/설명 표시
- 기존 아이콘 표시

5. 현재 categoryIcon 및 linkCategories 구조는 그대로 재사용할 수 있다면 재사용해주세요.
   DB의 category 값과 현재 UI의 category 필터가 연결되도록 해주세요.

6. 데이터 로딩 상태를 처리해주세요.
   Supabase에서 데이터를 가져오는 동안 기존 UI를 크게 변경하지 않는 선에서 적절한 로딩 표시를 보여주세요.

7. Supabase 조회에 실패했을 경우 빈 화면이 되지 않도록 에러 상태를 처리해주세요.
   사용자가 데이터를 불러오지 못했다는 것을 알 수 있는 정도의 간단한 오류 UI를 추가해주세요.

8. 중요한 기존 동작을 임의로 삭제하지 마세요.
   HomePage의 "내 북마크" 영역 등 bookmark_items와 직접 관계없는 기존 UI와 기능은 이번 단계에서 수정하지 마세요.

9. user_bookmarks 기능은 이번 단계에서 구현하지 마세요.
   이번에는 bookmark_items를 읽어서 화면에 표시하는 SELECT 기능만 구현합니다.

10. bookmark_items 데이터를 다시 INSERT하거나 DB 스키마를 변경하지 마세요.
    현재 DB 데이터를 그대로 사용해주세요.

11. 기존 하드코딩 데이터의 문자열 id(official, ticket 등)는 더 이상 필요하지 않습니다.
    화면에서 bookmark_items.id를 사용해주세요.
    단, 현재 UI의 다른 로직에서 기존 문자열 id를 참조하고 있다면 그 부분을 먼저 찾아서 보고하고, 임의로 깨뜨리지 마세요.

12. 코드 수정 후 다음을 확인해주세요.

- npm/Vite 환경에서 빌드 또는 개발 서버 실행에 문제가 없는지
- HomePage에서 Supabase SELECT가 실제로 실행되는지
- DB의 10개 bookmark_items가 화면에 표시되는지
- category 필터가 정상적으로 작동하는지
- 비로그인 상태에서도 bookmark_items를 읽을 수 있는지
  (RLS에서 anon SELECT를 허용했으므로 이 부분도 확인해주세요)

이번 단계에서는 다음 작업을 하지 마세요:

- user_bookmarks INSERT
- 폴더 기능
- 북마크 삭제
- 북마크 저장/취소
- DB 스키마 변경
- RLS 변경
- 회원가입/로그인 수정

작업 후에는 수정한 파일과 각 파일에서 무엇을 변경했는지,
그리고 실제 테스트 결과를 자세히 보고해주세요.
